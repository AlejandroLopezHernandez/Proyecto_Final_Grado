<form method="post" action="{{ path('app_producto_comida_delete', {'id': producto_comida.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ producto_comida.id) }}">
    <button class="btn">Delete</button>
</form>
